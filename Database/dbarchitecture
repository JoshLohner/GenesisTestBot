table_name = "templates"
    columns = {
        "templateid": "INTEGER PRIMARY KEY AUTOINCREMENT",
        "creator": "TEXT",
        "template": "TEXT NOT NULL UNIQUE",
        "assignedTo": "INTEGER"
    }